# Core
streamlit>=1.28.0
python-dotenv>=1.0.0

# PDF parsing
pypdf>=3.17.0

# Vector search (lightweight, works on all Python versions)
scikit-learn>=1.3.0
numpy>=1.24.0

# Spell correction for user queries
pyspellchecker>=0.7.0

# Google Gemini (free tier)
google-generativeai>=0.7.0

# Optional: OpenAI (only needed if OPENAI_API_KEY is set in .env)
openai>=1.6.0
