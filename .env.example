# DocuMind AI — Environment Variables
# Copy this file to .env and fill in your values.

# Optional: Add your OpenAI API key to use GPT-3.5-Turbo for better answers.
# Without this key, the app uses the free local Flan-T5-Small model.
OPENAI_API_KEY=
