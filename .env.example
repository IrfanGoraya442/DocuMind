# DocuMind AI — Environment Variables
# Copy this file to .env and fill in your values.

# Google Gemini API key (free tier) — get one at https://aistudio.google.com
GEMINI_API_KEY=

# Optional: OpenAI API key (paid) — used if GEMINI_API_KEY is not set
OPENAI_API_KEY=
